---
const { title } = Astro.props;
const base = import.meta.env.BASE_URL || "/";
---

<a class="font-display text-2xl font-bold title" href={base} title={title}
  >{
    title
      .split("")
      .map((letter: string) => <span class="title-letter">{letter}</span>)
  }
</a>
